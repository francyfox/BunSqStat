{
	"name": "server",
	"version": "1.0.50",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"dev": "bun --env-file=../../.env run --watch src/index.ts"
	},
	"dependencies": {
		"ajv": "^8.17.1",
		"chokidar": "^4.0.3",
		"elysia": "latest"
	},
	"devDependencies": {
		"bun-types": "latest"
	},
	"module": "src/index.js"
}
